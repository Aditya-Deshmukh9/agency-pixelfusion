/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost:3001"],
    domains: ["localhost", "localhost:3000"],
  },
};

// http://localhost:3000/images/e1.jpg

export default nextConfig;
